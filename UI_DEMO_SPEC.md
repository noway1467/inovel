# 悦读小说平台双端 UI Demo 规格

> 用途：产品原型、视觉设计、前端拆页与验收的统一依据。  
> 范围：Desktop、Tablet、Mobile；只描述页面与交互，不包含实现代码。

## 1. 视觉方向与 Token

平台是长时间使用的内容产品，视觉应安静、清晰、克制。读者前台强调内容与阅读，作者和后台强调信息效率，避免营销页式大 Hero、装饰渐变和卡片套卡片。

| Token | 建议值 |
|---|---|
| Brand | `#2563EB` |
| Accent | `#D97706`，只用于精选、更新等少量强调 |
| Success / Warning / Danger | `#15803D / #B45309 / #B91C1C` |
| Light Background / Surface | `#F6F7F9 / #FFFFFF` |
| Light Text | `#202124` |
| Dark Background / Surface | `#111315 / #191C1F` |
| Dark Text | `#E7E9EC` |
| Radius | 输入与按钮 6px，单项卡片 8px |
| Motion | 普通切换 120–200ms，翻页不超过 350ms |

- 平台 UI 字体使用系统字体；小说正文使用独立阅读字体栈。
- Desktop 页面最大内容宽 1200–1280px；阅读正文最大宽 720–860px。
- 所有图标按钮使用 Lucide，陌生图标提供 Tooltip 和可访问名称。
- 卡片只用于作品等独立重复项，不把整段页面区域浮成卡片。

## 2. 响应式基础

| 视口 | Demo 基准 | 行为 |
|---|---:|---|
| Mobile S | 320px | 单列、最紧凑、检查长文本和安全区 |
| Mobile | 390px | 主移动设计稿 |
| Tablet | 768px | 可收起侧栏、双列或单列增强 |
| Desktop | 1440px | 主桌面设计稿 |

每个核心页面至少提供 1440px 和 390px；阅读器额外提供 320px、768px、Mobile 横屏和 Dark/OLED 主题。

## 3. 全局导航

### Desktop

```text
┌────────────────────────────────────────────────────────────────────────────┐
│ [书本] 悦读小说网  首页  分类  榜单  [搜索书名/作者/标签____]  [主题] [头像] │
└────────────────────────────────────────────────────────────────────────────┘
```

- Header 高 56–64px，可固定。
- 搜索是中部最宽控件，聚焦显示历史、热门和联想。
- 用户菜单：个人中心、书架、阅读历史、作者工作台（有权限）、管理后台（有权限）、设置、退出。

### Mobile

```text
┌──────────────────────────────┐
│ [书] 悦读小说网       [搜] [头像]│
├──────────────────────────────┤
│             内容              │
├──────────────────────────────┤
│ [首页] [分类] [书架] [我的]     │
└──────────────────────────────┘
```

- 搜索进入独立全屏页，不在窄 Header 里堆输入框。
- 底部导航适配安全区域，在阅读器和编辑器内隐藏。
- 未登录的书架操作先保留意图，登录后继续完成。

## 4. 首页

### Desktop Demo

```text
┌──────────────┬──────────────────────────────────────────────────────────┐
│ 分类导航      │ 继续阅读：封面 / 书名 / 当前章节 / 进度 / [继续阅读]       │
│ 玄幻 奇幻     ├──────────────────────────────────────────────────────────┤
│ 武侠 仙侠     │ 编辑推荐：1 个主推荐 + 4 个紧凑作品                       │
│ 都市 言情     ├──────────────────────────────────────────────────────────┤
│ 历史 科幻     │ 最新上传：图文列表                    周榜/月榜 Tabs       │
│ ...          ├──────────────────────────────────────────────────────────┤
│ 热门标签      │ 分类精选：分频道横向书目                                 │
└──────────────┴──────────────────────────────────────────────────────────┘
```

- 有历史时优先显示继续阅读；无历史时显示新用户推荐。
- 推荐主视觉使用真实作品封面或作品相关图，不使用纯渐变占位。
- 加载骨架保持最终尺寸；单模块失败可原位重试。

### Mobile Demo

```text
┌──────────────────────────────┐
│ 继续阅读  书名              > │
│ 当前章节 · 38%       [继续]    │
├──────────────────────────────┤
│ 编辑推荐：横向可滑封面列表      │
├──────────────────────────────┤
│ 分类：2×4 文字/图标入口         │
├──────────────────────────────┤
│ 最新上传：单列紧凑书目           │
├──────────────────────────────┤
│ 榜单 Tabs：周榜 月榜 总榜        │
└──────────────────────────────┘
```

Mobile 不保留常驻分类侧栏；书卡主操作通过整行点击或明确按钮提供，不依赖 Hover。

## 5. 搜索、分类与榜单

### 搜索状态

- 初始：搜索历史、热门词、热门标签。
- 输入：作品、作者、标签联想；键盘可上下选择。
- 结果：关键词、数量、筛选、排序和结果列表。
- 无结果：纠错建议、相近标签、清除筛选、热门作品。
- 错误：保留关键词和筛选条件，提供重试。

Desktop 左侧固定筛选；Mobile 顶部“筛选/排序”打开底部抽屉，应用后显示筛选数量。

作品列表项包含封面、书名、作者、简介、标签、最新章节、更新时间和状态。Mobile 简介缩为两行，次级操作进入更多菜单。

## 6. 作品详情

### Desktop

```text
┌──────────────────────────────────────────────────────────────────────┐
│ [封面]  书名                                                         │
│         作者 · 分类 · 连载中 · 123 万字                               │
│         [标签] [标签] [标签]                                         │
│         简介最多 4 行 [展开]                                         │
│         [继续阅读] [加入书架] [分享] [举报]                           │
├──────────────────────────────────────────────────────────────────────┤
│ Tabs：[作品信息] [目录 328] [书评]                                    │
│ 最新章节 / 分卷目录（正序/倒序）                                      │
├──────────────────────────────────────────────────────────────────────┤
│ 作者信息                         同类推荐                             │
└──────────────────────────────────────────────────────────────────────┘
```

### Mobile

- 顶部紧凑展示封面和核心元信息，不制作占满首屏的营销 Hero。
- 底部固定 `继续阅读` 与 `加入书架`，适配安全区。
- 目录使用全屏页或底部抽屉，支持分卷、排序和当前章定位。
- 长简介默认折叠，展开后不遮挡主操作。

必须设计：未上架、审核中、已下架、无章节、章节全部锁定等状态。

## 7. 内置阅读器（核心 Demo）

### 7.1 Desktop 工具栏显示态

```text
┌─────────────────────────────────────────────────────────────────────────┐
│ [返回] 书名 / 第十二章 夜雨                         [目录] [设置] [全屏] │
├──────────┬──────────────────────────────────────────────────────────────┤
│ 目录      │                    第十二章 夜雨                            │
│ 书签      │                                                            │
│ 笔记 P1   │ 第一段正文……                                               │
│ 夜间      │                                                            │
│           │ 第二段正文……                                               │
├──────────┴──────────────────────────────────────────────────────────────┤
│ 上一章        本章 45% · 全书 18% [────●────]                 下一章    │
└─────────────────────────────────────────────────────────────────────────┘
```

- 正文居中，工具栏交互后出现，数秒无操作淡出。
- 翻页模式下左右点击区翻页，中部点击呼出工具栏。
- 滚动模式不把正文塞进有边框的装饰卡片。
- 选择正文后 P1 显示划线、笔记、复制工具，不覆盖选区。

### 7.2 Mobile 工具栏显示态

```text
┌──────────────────────────────┐
│ 书名 · 第十二章 夜雨          │
├──────────────────────────────┤
│                              │
│          正文阅读区域          │
│                              │
│ 左侧点击   中部菜单   右侧点击 │
│                              │
├──────────────────────────────┤
│ 18%  [──────●────────]        │
│ [目录] [夜间] [设置] [更多]     │
└──────────────────────────────┘
```

- 隐藏态正文延伸到安全区域，仍保留可读边距。
- 横向滑动只在覆盖/仿真模式工作；滚动模式不拦截纵向手势。
- 字体放大至 200% 时操作进入更多或换行，不能盖住正文。
- 横屏时正文列宽受限，工具栏可移到侧边。

### 7.3 阅读设置面板

Desktop 为右侧 Drawer，Mobile 为底部 Drawer：

```text
阅读设置
主题  [明亮] [柔和] [羊皮纸] [墨灰] [OLED]
字号  [−] 18 [+]
字体  [系统字体 v]
行距  [紧凑 ───●── 宽松]
段距  [无] [小] [中] [大]
边距  [窄] [标准] [宽]
对齐  [两端] [左对齐]
缩进  [无] [2 字符]
翻页  [滚动] [覆盖] [仿真 P1] [无动画]
动画  [跟随系统 / 开 / 关]
[恢复默认]
```

- 主题不仅显示颜色块，还需有名称和选中状态。
- 每次修改即时预览并保存；调整后保持当前段落锚点。
- 羊皮纸纹理低对比，不影响文字选择和可读性。

### 7.4 目录、书签与笔记

- Tabs：目录、书签、笔记。
- 目录标记当前、已读、锁定、下架章节；分卷可折叠。
- 书签展示章节、摘要、创建时间，点击回到锚点。
- P1 笔记展示划线和备注，可编辑、删除、按章节筛选。
- 删除提供撤销窗口；同步失败显示“已保存在本机”。

### 7.5 阅读状态

- 首次加载：先应用已保存主题，再显示章节骨架，避免白屏闪烁。
- 离线：已缓存章节可读，提示进度保存在本机。
- 正文失败：保持阅读器外壳和当前位置，提供重试。
- 章节下架：说明原因并提供目录/下一可读章。
- 多端冲突：显示另一设备位置，允许跳转或保留当前。
- 性能不足：仿真自动降级到覆盖并轻提示。
- 全书读完：完成状态、返回详情、书评与相关推荐。

## 8. 我的书架

### Desktop

- 工具条：书架分组、排序、筛选、列表/网格、批量管理。
- 列表项：封面、书名/作者、最新章节、最后阅读、进度、更新时间、操作菜单。
- 批量模式显示复选框和底部操作条。

### Mobile

- 顶部为分组 Tabs；第二行排序、筛选和视图切换。
- 列表卡突出封面、书名、进度和继续阅读，其余进入更多菜单。
- 网格根据宽度稳定显示 2 或 3 列，书名最多两行，卡高一致。
- 长按可进入批量模式，但仍提供可发现的“管理”按钮。

必须设计：空书架、无筛选结果、作品更新、作品下架、同步失败。

## 9. 个人中心

Desktop：左侧个人导航，右侧内容。Mobile：入口列表进入独立二级页。

- 个人资料：头像、昵称、简介。
- 阅读偏好：默认主题、翻页、字号、行距、同步。
- 账号安全：邮箱、密码、登录设备、2FA（高权限）。
- 通知设置：作品更新、互动、系统、审核反馈。
- 阅读数据：阅读时长、天数、字数等可选展示，说明统计口径。

## 10. 作者工作台

### 10.1 作品列表

- Desktop 左侧导航；Mobile 单列作品卡。
- 每部作品展示封面、状态、章节数、字数、最近保存/发布、待处理审核意见。
- 主操作依据状态变化：继续编辑、查看审核、发布设置、查看作品。

### 10.2 新建作品

采用独立页面的分区表单：基本信息、分类标签、封面、版权与发布。不要把所有字段塞进小弹窗。

### 10.3 导入向导

```text
1 选择 TXT → 2 上传/解析 → 3 章节预览修正 → 4 确认导入 → 5 结果
```

预览必须展示：识别规则、编码、未识别段落、重复标题、超长章节、合并/拆分/忽略操作。

### 10.4 章节编辑器 Desktop

```text
┌────────章节树────────┬────────────────────────────────────────────────┐
│ 搜索 / 新建章节       │ 章节标题 [________________________]  保存状态    │
│ 卷一                  ├────────────────────────────────────────────────┤
│  第1章                │ [B] [I] [U] [对齐] [自动排版] [版本历史]         │
│  第2章 当前           ├────────────────────────────────────────────────┤
│ 卷二                  │                                                │
│                       │                 正文编辑区                       │
│                       │                                                │
├───────────────────────┴────────────────────────────────────────────────┤
│ 字数 / 上次保存                             [存草稿] [预览] [提交审核] │
└────────────────────────────────────────────────────────────────────────┘
```

### 10.5 章节编辑器 Mobile

- 顶部：返回、章节名、保存状态、更多。
- 章节树改全屏抽屉，编辑区单列。
- 工具栏横向滚动，常用动作保持第一层。
- 软键盘打开时底部栏缩为保存与更多，不遮挡编辑位置。

必须设计：未保存、保存中、已保存、失败、版本冲突、审核中、退回、定时发布。

## 11. 管理后台

### Desktop

- 左侧导航 224–256px，可收起为图标栏。
- 顶部为面包屑、任务通知、管理员菜单。
- 表格支持筛选、排序、分页、列设置、批量操作。
- 长正文审核使用独立页面或宽 Drawer，不在小弹窗审稿。

### Mobile

- 导航改抽屉；首页先显示待办和异常摘要。
- 表格行转信息卡或清晰横向滚动，不能把列压成碎字。
- 审核底部固定：通过、退回、拒绝、升级复审。

### 审核详情

```text
作品/章节上下文 | 作者历史 | 自动规则命中
────────────────────────────────────
正文与上一版本差异、问题位置标注
────────────────────────────────────
审核意见
[通过] [退回修改] [拒绝] [升级复审]
```

退回、拒绝、下架必须填写原因；危险操作与普通主按钮视觉区分。

## 12. 组件全状态

每个页面至少覆盖：

- Default、Hover（如适用）、Focus、Active、Disabled。
- Loading、Empty、Error、Offline、No Permission。
- 长书名、长作者名、多标签、无封面、超长章节标题。
- 游客、读者、作者、审核员、管理员的可见差异。
- 平台 Light/Dark 与阅读器各主题。

表单额外覆盖：必填、格式错误、服务端冲突、提交中、成功、草稿恢复。

## 13. Demo 验收

1. 所有核心页有 1440px Desktop 与 390px Mobile 设计稿。
2. 阅读器有 320px、768px、横屏 Mobile、Dark/OLED 和羊皮纸稿。
3. 固定栏、Drawer、软键盘与安全区域不遮挡内容。
4. 中文真实长文本下按钮、卡片、表格、导航不溢出。
5. 原型可完成：搜索到阅读、继续阅读、TXT 导入到审核、审核退回再提交。
6. 所有图标操作有 Tooltip/可访问名称，键盘焦点顺序合理。
7. 标注每块数据来源、权限、空状态、错误状态和后台配置来源。

