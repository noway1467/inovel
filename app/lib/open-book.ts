/**
 * 从列表里点开一本书时用的链接属性。
 *
 * 为什么开新标签页：书架、发现、搜索结果、榜单都是"逛"的场景 —— 用户翻了
 * 半屏挑中一本，在原页打开就把滚动位置和筛选条件一起丢了，回来还得重挑。
 * 书是终点页，逛的页面是索引页，索引页留着才顺手。
 *
 * 只用在"从索引进书"这一步。书内的导航（上一章/下一章、目录抽屉、开始阅读）
 * 照旧原地跳转，否则读十章就攒出十个标签页。
 *
 * `rel` 是同源也照写：`noopener` 断开新页对 `window.opener` 的引用，
 * 站内出现一次外链或反代时不至于漏个抓手出去。
 */
export const openBookLinkProps = {
  target: "_blank",
  rel: "noopener noreferrer",
} as const;
