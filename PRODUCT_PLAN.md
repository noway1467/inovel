# 悦读小说平台完整产品规划

> 版本：v1.0  
> 日期：2026-08-13  
> 目标终端：Desktop Web、Tablet Web、Mobile Web  
> 技术基线：Cloudflare Workers + D1 + R2  
> 文档性质：产品与项目执行规划，不包含实现代码。

## 1. 产品定义

悦读小说平台是一套覆盖内容发现、在线阅读、个人书架、作者创作、内容审核和平台运营的在线小说系统。产品核心是“稳定、连续、舒适的阅读闭环”，而不是只把小说文件摆成列表。

### 1.1 目标用户

- 游客：搜索、浏览、试读公开作品。
- 注册读者：收藏、同步进度、管理书签笔记和阅读历史。
- VIP 用户：预留会员权益，不将 VIP 混入后台管理权限。
- 作者：建书、导入、编辑、提交审核、查看反馈和作品数据。
- 审核员：审核作品与章节、处理举报、标注风险。
- 运营：维护分类标签、推荐位、榜单、公告和活动。
- 管理员：管理用户、权限、作品状态、系统配置和审计。

### 1.2 产品原则

1. 阅读优先，页面最终要帮助用户更快找到书并继续阅读。
2. Desktop 与 Mobile 能力一致，但导航、筛选、弹层和操作方式分别设计。
3. 所有用户资产可恢复：进度、书架、书签、笔记、偏好、草稿。
4. 作品与章节有完整生命周期，不允许作者绕过审核直接公开。
5. 内容、账号和管理操作遵循最小权限并保留审计。
6. MVP 先稳定内容与阅读闭环，付费、分成和 DRM 独立立项。

## 2. 范围

### 2.1 MVP 必须包含

- 首页、分类、榜单、搜索、作品详情、作者主页。
- 登录注册、个人中心、通知、阅读历史、我的书架。
- 内置阅读器：滚动、左右覆盖、无动画翻页；多主题与完整排版设置。
- 阅读目录、书签、进度同步、跨设备恢复。
- 作者工作台、新建作品、TXT 导入、章节编辑、草稿、提交审核。
- 管理后台：用户、角色、作品、章节审核、分类标签、推荐位、公告、日志。
- Desktop、Tablet、Mobile 响应式 UI 和真实交互验收。
- Cloudflare Workers、D1、R2、Queues 的部署和运维方案。

### 2.2 P1 增强

- 仿真翻页、划线笔记、TTS、书架自定义分组。
- DOCX 导入、章节版本对比、定时发布。
- 书评、回复、举报、申诉和通知偏好。
- 编辑推荐、周/月/总榜、基础个性化推荐。

### 2.3 P2 与独立项目

- 付费章节、充值、订单、提现、作者分成。
- 原生 App、复杂离线下载、DRM 和版权水印。
- AI 写作、AI 配图、复杂社区和社交关系。

## 3. 信息架构

```text
读者前台
├─ 首页
├─ 分类与标签
├─ 榜单
├─ 搜索与搜索结果
├─ 作品详情
│  ├─ 作品信息
│  ├─ 章节目录
│  ├─ 书评
│  └─ 相关推荐
├─ 内置阅读器
└─ 作者主页

个人域
├─ 我的书架
├─ 阅读历史
├─ 书签与笔记
├─ 通知中心
├─ 个人资料
├─ 阅读偏好
└─ 账号安全

作者工作台
├─ 概览
├─ 我的作品
├─ 新建作品
├─ 文件导入
├─ 章节编辑器
├─ 发布与审核记录
└─ 作品数据

管理后台
├─ 仪表盘
├─ 用户与权限
├─ 作者认证
├─ 作品与章节
├─ 审核与举报
├─ 分类与标签
├─ 推荐位与榜单
├─ 公告与通知
├─ UI 功能配置
├─ 系统任务与缓存
└─ 审计、登录与异常日志
```

## 4. 角色权限矩阵

系统采用 RBAC，一个账号可同时拥有多个角色。

| 能力 | 游客 | 读者 | 作者 | 审核员 | 运营 | 管理员 | 超管 |
|---|---|---|---|---|---|---|---|
| 浏览与阅读公开内容 | 是 | 是 | 是 | 是 | 是 | 是 | 是 |
| 云同步进度/书架/书签 | 否 | 是 | 是 | 是 | 是 | 是 | 是 |
| 创建和编辑自己的作品 | 否 | 否 | 是 | 否 | 否 | 可干预 | 可干预 |
| 审核作品和章节 | 否 | 否 | 否 | 是 | 只读 | 是 | 是 |
| 维护推荐位和榜单 | 否 | 否 | 否 | 否 | 是 | 是 | 是 |
| 管理用户和角色 | 否 | 否 | 否 | 否 | 否 | 是 | 是 |
| 修改核心权限和密钥 | 否 | 否 | 否 | 否 | 否 | 否 | 是 |

约束：

- 作者只能操作自己拥有或被授权协作的作品。
- 审核员不能直接改作者正文，只能通过审核意见推动修改。
- 封禁、下架、删除、角色变更和配置变更必须记录前后值与原因。
- 超级管理员启用二次验证，不用于日常运营。

## 5. 核心业务流程

### 5.1 读者发现与阅读

```text
首页/分类/榜单/搜索
→ 作品详情
→ 开始或继续阅读
→ 阅读器加载偏好与进度
→ 阅读、调节排版、书签
→ 本地高频保存
→ 云端节流同步
→ 书架与历史更新
```

### 5.2 作者投稿

```text
创建作品草稿
→ 填写书名/简介/分类/标签/封面
→ 手工建章或上传 TXT
→ 编码与章节识别
→ 作者预览、合并、拆分、重命名
→ 导入章节草稿
→ 编辑与自动保存
→ 提交审核
→ 通过后立即/定时发布，或退回后修改再提交
```

### 5.3 审核治理

```text
章节进入待审核队列
→ 自动规则扫描
→ 审核员查看正文、差异、作者历史
→ 通过 / 退回 / 拒绝 / 升级复审
→ 记录意见和审计
→ 通知作者
→ 发布或重新修改
```

## 6. 页面功能规格

### 6.1 首页

- 固定 Header：Logo、主频道、搜索、日夜模式、用户菜单。
- 登录用户优先显示“继续阅读”，游客显示编辑推荐。
- 最新上传、分类精选、周/月榜、热门标签、编辑推荐。
- 作品卡展示封面、书名、作者、分类、状态、最新章节和更新时间。
- 推荐位由后台控制排序、时间和上下线，不允许随意破坏页面结构。

### 6.2 搜索、分类与榜单

- 搜索书名、作者、标签；输入时显示历史、热门词和联想。
- 结果支持分类、标签、连载状态、字数、更新时间筛选。
- 无结果显示纠错建议、相近标签和热门作品。
- 分类支持主分类、子分类和标签组合。
- 榜单必须公开统计口径，后台可冻结异常数据。

### 6.3 作品详情

- 封面、书名、作者、分类、标签、字数、状态、更新时间、简介。
- 主操作：开始/继续阅读、加入书架；次操作：分享、举报。
- 最新章节、分卷目录、正序/倒序、当前阅读章节定位。
- 作者信息、作者其他作品、书评、同类推荐。
- 审核中、下架、章节锁定等状态有明确说明和可行下一步。

### 6.4 账号与个人中心

- 用户名或邮箱注册，手机号为可选扩展。
- 密码登录、忘记密码、设备列表、退出其他设备。
- 账号标识与公开昵称分离，不公开登录账号。
- 阅读偏好、通知设置、资料编辑和账号注销流程。
- 通知分为系统、作品更新、审核反馈和互动。

### 6.5 书架与历史

- 列表/网格视图、排序、筛选、分页或游标加载。
- 默认最近阅读；支持最近更新、加入时间、书名排序。
- 展示最后阅读章节、进度、最新章节和更新状态。
- P1 支持“正在追、稍后读、已读完”等自定义分组。
- 移出书架默认不删除历史、书签和笔记。

### 6.6 作者工作台

- 作品列表展示状态、章节、字数、更新时间、待处理审核意见。
- 新建作品字段：书名、封面、简介、主/子分类、标签、连载状态、版权声明。
- TXT 导入流程：上传、编码识别、章节识别、预览修正、确认导入。
- 编辑器支持分卷章节树、搜索、排序、受限格式、字数、自动保存、版本历史。
- 自动规范标题和正文排版必须先预览差异，再确认应用。
- “发布”实际为提交审核，通过后才公开。

### 6.7 管理后台

- 仪表盘显示待审核、发布趋势、活跃用户、失败任务和异常告警。
- 用户管理支持搜索、角色、状态、禁言/封禁期限和历史。
- 审核页展示正文差异、自动规则命中、历史结论和审核意见。
- 分类标签支持别名、排序、启停、合并，删除前检查引用。
- UI 配置只开放白名单项：主题默认值、模块开关、Banner、公告、推荐位。
- 缓存清理按缓存域执行，显示影响范围，禁止模糊“一键清空全部”。

## 7. 内置阅读器完整规格

### 7.1 阅读模式

- 上下滚动：全端默认、最稳定。
- 左右覆盖：按逻辑页切换，支持键鼠与触控。
- 无动画：低性能与无障碍回退模式。
- 仿真翻页：P1 增强，仅在性能和系统减少动态效果允许时启用。

章节是服务端内容边界；“页”由视口、字体、字号、行距和边距动态计算，不能作为跨设备进度主键。

### 7.2 阅读主题

- 明亮纸张：中性浅色。
- 柔和阅读：低对比暖灰绿，参考成熟阅读体验但不复制品牌资产。
- 羊皮纸：低饱和米黄色，纹理必须非常轻。
- 墨水灰：深灰背景，避免纯黑白强反差。
- OLED 黑：纯黑背景。
- 自定义：背景和文字色需通过对比度检查。

### 7.3 排版

- 字体：系统、宋体阅读、黑体阅读及安全回退。
- 字号：14–32px。
- 行距：1.4–2.4。
- 段距：0–1.5em。
- 页边距/正文宽度：窄、标准、宽。
- 对齐：两端、左对齐。
- 段首缩进：无、2 字符。
- 字间距：默认、宽松两个安全档。

### 7.4 阅读工具

- 目录、分卷、上一章/下一章、章节跳转。
- 书签；P1 增加划线、笔记和 TTS。
- 章节与全书进度滑杆，跳转前显示目标章节。
- 全屏、自动隐藏工具栏、章节举报。
- 浏览器能力允许时可保持屏幕常亮。

### 7.5 双端交互

- Desktop：方向键/PageUp/PageDown 翻页或滚动，Esc 关闭面板，F 全屏，M 书签。
- Mobile：中部点击呼出菜单，左右区域点击翻页，横向滑动翻页。
- 滚动模式不拦截垂直系统手势。
- 输入笔记时停用翻页快捷键。
- 系统开启减少动态效果时自动降级动画。

### 7.6 阅读进度

服务端进度模型：

```text
userId + bookId + chapterId + paragraphAnchor + charOffset
+ chapterProgress + bookProgress + updatedAt + deviceId
```

- 本地高频保存，服务端节流写入。
- 切章、切后台、关闭页面时立即尝试同步。
- 多端冲突默认提示更新较新的位置，并允许保留当前或跳转。
- 匿名用户本地保存，登录后询问是否合并。
- 正文改版导致锚点失效时按相邻锚点和字符比例降级恢复。

## 8. 状态机

```text
作品：draft → pending_review → approved → published
                         ↘ rejected
published → suspended → published
published → archived

章节：draft → pending_review → approved → published
                         ↘ rejected → draft
approved → scheduled → published
published → hidden → published

导入任务：uploaded → parsing → awaiting_confirmation → importing → completed
                   ↘ failed                    ↘ failed
```

任何退回、拒绝、下架、封禁都必须包含原因；高风险内容进入双人复审。

## 9. 双端响应式标准

| 终端 | 宽度 | 主要策略 |
|---|---:|---|
| Mobile S | 320–374 | 单列、底部操作、全屏搜索与抽屉 |
| Mobile L | 375–767 | 单列、触控优先、阅读器沉浸 |
| Tablet | 768–1023 | 双列或可收起侧栏 |
| Desktop | 1024–1439 | 标准双/三栏、固定侧栏 |
| Wide | ≥1440 | 限制最大宽度，不无限拉伸正文 |

- Desktop：顶部主导航；作者与后台使用左侧栏。
- Mobile：顶部保留品牌、搜索、用户入口；底部导航为首页、分类、书架、我的。
- 搜索在 Mobile 为独立全屏页，筛选为底部抽屉。
- 编辑器章节树在 Mobile 为全屏抽屉。
- 后台 Mobile 以查看、审核和紧急处理为主，复杂表格转卡片或横向滚动。
- 主要触控目标至少 44×44 CSS px。
- 固定底栏适配安全区域，Hover 不承担唯一操作。

## 10. 非功能要求

### 10.1 性能

- 首页、详情和章节优先输出关键内容，图片响应式加载。
- 正文按章读取，不一次加载整本。
- 超长章采用流式响应和分段渲染，不在 Worker 内存缓冲完整大文件。
- 搜索联想防抖并取消过期请求。
- 调整字号和主题不产生明显布局闪烁。
- 目标：LCP ≤ 2.5s、INP ≤ 200ms、CLS ≤ 0.1，以实测监控为准。

### 10.2 安全

- 成熟密码哈希、安全 Cookie、CSRF/XSS/注入/越权防护。
- 上传文件服务端校验类型、尺寸、编码，不执行宏与脚本。
- 富文本使用白名单清洗，正文保存为受限语义结构。
- 后台、登录和高频接口配置 Cloudflare WAF/Rate Limiting 规则。
- 日志不得写密码、完整令牌、私密草稿或不必要个人信息。

### 10.3 可访问性

- 以 WCAG 2.2 AA 核心要求为目标。
- 键盘可完成所有主操作，焦点清晰，弹层管理焦点。
- 颜色不是唯一状态提示，图标按钮有可访问名称。
- 阅读器支持减少动态效果、字号放大和清晰章节结构。

### 10.4 可靠性

- 关键任务支持重试、死信和人工重跑。
- D1 使用迁移和 Time Travel；R2 开启生命周期与必要版本策略。
- 发布有 Preview、Staging、Production 环境和回滚流程。
- 监控接口错误率、D1 查询、队列积压、导入失败、R2 错误和审核积压。

## 11. 分期计划

### Phase 0：产品、原型与架构（2 周）

- 完成 PRD、状态机、字段字典、API 契约。
- 完成 Desktop/Mobile 高保真原型和设计 Token。
- 建立 Cloudflare 资源规划、环境和迁移策略。
- 用真实长书名、简介、章节和错误 TXT 验证原型。

### Phase 1：基础平台（2 周）

- Worker 全栈骨架、D1 迁移、R2、Queues、鉴权、RBAC。
- 全局导航、主题、响应式框架、错误与日志基础设施。

### Phase 2：读者闭环（3–4 周）

- 首页、搜索、分类、详情、阅读器、书架、历史、进度同步。
- Desktop/Mobile 真实浏览器验收。

### Phase 3：作者与审核（3–4 周）

- 新建作品、TXT 导入、章节编辑、版本、审核和通知。
- 后台用户、作品、分类标签、推荐位和审计。

### Phase 4：上线准备（1–2 周）

- 性能、安全、无障碍、兼容性、备份恢复和灰度。
- 运营数据、审核规范、告警和回滚演练。

## 12. 核心验收

### 12.1 读者

1. Desktop 与 Mobile 均可从搜索进入详情并开始阅读。
2. 阅读主题、字号、行距等刷新后不丢失。
3. 跨章节、刷新、换设备后恢复到稳定段落。
4. 320px 宽度无横向溢出、按钮与正文不重叠。
5. 横竖屏切换后阅读锚点不明显漂移。

### 12.2 作者

1. 上传 UTF-8/GB18030 TXT 后可预览和修正章节识别。
2. 自动保存状态明确，断网恢复不丢失已确认内容。
3. 作者无法绕过审核直接公开章节。
4. 审核退回能定位章节、查看原因并再次提交。

### 12.3 管理与平台

1. 越权访问被拒绝并生成可追踪日志。
2. 角色变更、封禁、下架、配置修改可审计。
3. 队列重复投递不会重复发布、重复通知或重复写入。
4. 超大/恶意/错误编码文件不会拖垮 Worker。
5. D1、R2 和队列失败均有重试、告警或人工处理入口。

### 12.4 兼容性矩阵

- Desktop：Chrome、Edge、Firefox、Safari 最近两个稳定大版本。
- Mobile：iOS Safari、Android Chrome 最近两个主要版本。
- 视口：320×568、375×812、390×844、768×1024、1366×768、1440×900。
- 输入：鼠标、键盘、触控。

## 13. 完成定义

功能同时满足以下条件才算完成：

- 字段、权限、加载、空、错误、离线和无权限状态已定义。
- Desktop、Tablet、Mobile 布局和交互通过验收。
- API 校验、幂等、审计和失败恢复已覆盖。
- 单元、集成和真实浏览器 E2E 测试通过。
- 监控、日志、隐私、部署与回滚说明已更新。
- 没有用静态假数据页面冒充完整业务闭环。
